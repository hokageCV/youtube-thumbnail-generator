import { Window } from '@/components/window';

export default function Home() {
  return (
    <div className="flex flex-col md:flex-row w-full max-w-5xl min-h-screen overflow-hidden">
      <Window />
    </div>
  );
}
